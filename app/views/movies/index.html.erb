<% @movies.each do |movie| %>
 <%= link_to movie.name, movie_path(movie) %>
 <%= link_to 'düzenle', edit_movie_path(movie) %>
 <%= link_to 'Sil', movie_path(movie), method: :delete, data: {confirm:'Emin misin?'}  %>
 <br>
<% end %>